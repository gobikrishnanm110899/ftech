<IfModule mod_rewrite.c>
    RewriteEngine On

    # If this project is hosted with the Laravel folder as the web root (common in XAMPP),
    # forward all requests to /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

